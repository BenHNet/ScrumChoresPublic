(function (window, undefined) {

    var appScrumChoresConfig = {

        //kendoui router object
        baseUrl: "http://localhost:6155/"

    };

    return (window.appScrumChoresConfig = appScrumChoresConfig);

}(window));